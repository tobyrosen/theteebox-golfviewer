name: CI
# Thin caller -> shared reusable workflow (tobyrosen/fleet-ci@v1). This repo's last CI commit.
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  ci:
    uses: tobyrosen/fleet-ci/.github/workflows/ci.yml@v1
